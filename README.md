# Barcode AI Telegram Bot

Telegram bot that reads product labels from photos, extracts code, barcode, name, price and quantity, asks for confirmation, and stores confirmed rows in Excel.

Set environment variable `BOT_TOKEN` with the token from BotFather, then run the Docker container.
